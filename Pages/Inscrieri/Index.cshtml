@page
@model proiect.Pages.Inscrieri.IndexModel

@{
    ViewData["Title"] = "Index";
    Layout = "~/Pages/Shared/_Layout.cshtml";
}
<style>
    body {
        background-color: AntiqueWhite; 
    }
</style>


<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Inscriere[0].Client.ClientName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Inscriere[0].Curs.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Inscriere[0].TipBilet.BiletType)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Inscriere) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Client.ClientName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Curs.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TipBilet.BiletType)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
